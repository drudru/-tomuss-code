Finir par priorité :
	TESTER : problème des extensions éditables
		Solution : Changer la direction du lien symbolique
		Ecrire le script d'inversion
		Changer le plugin extension
		Interdir l'écriture dans les liens symboliques (redirection?)
	COW (stable) valeur vide ==> comme si rien
	Sauvegarde de l'état figé...
	Consultation TT par les secrétaires
	Faire mail quand les scripts de nuit marchent pas.
	SUIVI étudiants : pointeurs SPIRAL/Mails enseignants ?
	Reconnexion au serveur quand la connexion est perdue
	Bug ajoute + vidage de ligne avec changement de page
	BILAN : probleme icone+note sous IE
	RSS table : TT DA, ajouter + info dans le texte et le sujet ?
	Relancer seulement le suivi ou tomuss dans Makefile
	Nom de jeune fille
	Mettre UNE table en lecture seulement : Envoyer mail a l'administratif
	linear	Afficher aide plus detaille sur la valeur de la cellule
	linear  rendre champs saisie invisible
	live_status : pong ne doit pas mettre la date courante si existe pas
	live_status : écarter les noeuds collés.
	live_status : afficher navigateur
	Il faut enlever toute référence aux noms de types dans le JS
	Ajouter les types Mail/telephone/responsable(UE)/enumeration
	Calcul du poids automatique avec '?' : problème de ]1,1[, max, meilleur
	Faire la doc ]1,1[
	Renommer Inscrit en _Inscrit_, garder la compatibilité pour les vieux.
	Faire le ménage des fonctions deprecated dans les TEMPLATES
	Faire des plugins pour chaque fonctionnalité coté client.

    BUGS :
	Chrome : statistics per group
	liste des UE dans le suivi, change au rechargement ???
	Copier puis drag fait n'importe quoi sur les cases du tableau.
		Sous windows seulement
	Fuite mémoire ?
	BUG : Le 'resume' permet d'avoir des informations non autorisée
	BUG : Libérer les caches ZERO arguments ???? Quand ???
	BUG : Pourquoi TOMUSS charge toutes les UE (3500) !!!
	Safari : requetes en double, page accueil
	chrome : requetes en double (changement de valeur cellule)
	Il n'y a pas de Flux RSS pour les tables ni printemps ni automne.

    Moyenne :
	Indicateur modifiable : official_ue
	Interface pour détruire les tables.
	UE par formation
	Interdire stockage mauvais encodage (liste étudiants ou autres)
	Trace des actions TOMUSS stockée dans le navigateur.
	Interface pour faire les extractions ?
	Lien sur l'envoi de mail aux étudiants d'une UE sur la page d'accueil
	Problème ascenseur qui apparait home page avec le + a droite
	Python 3
	Vérifier les conflits : identité cookie / ticket (voir mails) ?
	Histogramme : échelle log sur clique
	Statistiques : droite de régression
	Affichage des espaces dans les cases du tableau.
	Faire afficher l'historique dans un bulle (3 historiques donc ?)
	Meta log à distance : stream
	Chiffrer colonne.
	Importation de colonnes d'autres tableaux : nécessite calcul serveur
	Statistique par groupe sur une colonne quelconque

    Faible :
	stat : lien vers le suivi de la personne
	Évidence ligne courante : lignes bleu verticales et horizontales
	Mettre du style CSS dans le commentaire pour colorier la cellule ?
	regtest javascript : filter, double clique
	Faire un tip deuxième niveau (+ en haut/gauche) XXX dans utilities.js
	Autoriser n'importe qui à modifier les infos des autres (tous respons.)
	Espionner les filtres
	Version ligne de commande qui liste les personnes connectées
	WII : home.py : Largeur champ saisie est nul
	WII : largeur colonne tableau principal
	Tip de connexion ?	
	ABJ/DA : interdir de les faire pour l'année universitaire suivante
	Vérifier si les options sont envoyée dans les URL vers le serveur =xxx=
	Vue de la table telle qu'elle était à une date donnée.
	Type de colonne : remplace    filtre: valeur
	Tester le nouveau utility.js()
	Trombinoscope enseignant : voir boite a outil Intranet UCBL
	Ajouter des droits d'accès {'UE-MAT': 'philippe.malbos'}

    Amusant :
	Surveillance actions client envoyées sur onunload
	Histogramme date

Questions en suspend :
	Si =read-only- : ouvrir en read-only ?
	Mettre en évidence la ligne courante ?
	Pas de feedback quand ticket expiré ?
	Nouvelle page quand on clique dans le suivi ?
	Wizard création table ?
	Téléphone dans suivi et fiche ?
	Détruire les lignes vides ?
	Message si la liste des mails n'est pas a jour ????
	Faire des threads dans suivi ?
	Temps d'apparition des tips.
	Symbole Excel pour commentaire ?
	Un utilisateur 'root' pouvant modifier toutes les valeurs ?
	Faire erreur gentille sur mauvaise URL (sans année).	
	Lire responsable dans GASEL ?
	Déterminer plus fiablement quand un serveur a planté.
	Mettre le ticket en fin d'URL ?

BUGS/Problèmes en suspend :
	Problème accents dans opera : filtre et accueil : diacritics
	Pouvoir patcher une table .py sans arrêter TOMUSS
	ALT-LEFT : marche pas partout :-(
	<10 trouve les cases vides ? Faut-il corriger ?
	ABI => ABI et ne met pas ABINJ car INPUT change pas de valeur
		
	
Amélioration du code :
	La gestion de _url_ dans StaticFile et de BASE est NULLE........
	Faire proprement StaticFile
	Devrait faire une erreur quand on créer une année/semestres inexistant
	Dans bookmark : utiliser des ID plutot que le numéro de colonne
	Faire de l'UTF8 partout en interne
	Faire des objets JavaScript : ***Columns***, Line, Table, Tip...
	Javascript : supprimer data_col data_lin et utiliser les id directement
	Préfixer les variables globales par _ ou les supprimer (ou objet)
	Enlever ce qui est devenu inutile.
	Enlever les liens symboliques : trop de problèmes potentiels

NE SERA PAS FAIT :
	Dans SUIVI : mettre directement les commentaires visibles
		en effet, l'étudiant n'a pas à les voir par dessus
		l'épaule du référent.
	Enlever le javascript 'go()' de la page accueil : problème year/semest
	Passer directement les fichiers UE à Javascript :
		En effet, le serveur a BESOIN de les évaluer de toute façon.
	On ne peut pas ouvrir 2* la même page simultanément avec le
		même ticket car le navigateur croit que c'est la même.
	Importation de colonnes venant du web par le client : IMPOSSIBLE 
	Unifier les 'tip' : 2 tips simultanés possibles : photo + ABJ
	Import / Export de visibility_date pour la liste des colonnes
	Récupérer automatiquement openue.xls
		Il faut passer par gnuméric pour l'assainir.
	Prendre les mail étudiants de SPIRAL : pas politiquement correcte



